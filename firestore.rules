rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC ACCESS: Rules have been removed to allow full read/write access.
    // Use this for rapid development and testing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
